# /components/form

Will hold: `Form.Wrapper`, `Form.Field` (input + textarea variants),
`Form.ObjectReference`, `Form.ValidationMessage`, `Form.Confirmation` —
see Component Library Ch.11.

Not built yet (Production Architecture Plan, Phase 13). The matching Zod
schema lives in `/lib` and is shared by both this component tree and the
Route Handler — one schema, two consumers, per Production Architecture
Plan Ch.13.
