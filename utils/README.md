# /utils

Small, pure, stateless helper functions only — zero side effects, zero
imports beyond the standard library. If a function needs to talk to
Lenis, GSAP, or an API, it belongs in `/lib`, not here.

Nothing here yet — no pure-function need has arisen before any real
components exist.
